targetted_decisions = {
	Take_a_Hostage = {
		filter = court
		ai_target_filter = court

		from_potential = {
			ai = no
		}

		potential = {
			NOT = { age = 14 }
			prisoner = yes
			is_ruler = no
			host = { character = FROM }
		}

		allow = {
			NOT = { employer = { character= FROM } }
		}

		effect = {
			move_character = FROM
			opinion = { modifier = opinion_imprisoned who = FROM days = 3650 }
			prisoner = no
			set_character_flag = taken_hostage
		}
		ai_will_do = {
			factor = 0
		}
	}

}